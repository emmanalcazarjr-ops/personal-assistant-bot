{
  "name": "personal-assistant-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Personal Telegram assistant: AI chat with memory (DeepSeek), notes, reminders, daily briefing. grammY + Obsidian vault on Vercel.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx scripts/dev.ts",
    "typecheck": "tsc --noEmit",
    "set-webhook": "node scripts/set-webhook.mjs",
    "get-webhook": "node scripts/get-webhook.mjs"
  },
  "dependencies": {
    "grammy": "^1.33.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
